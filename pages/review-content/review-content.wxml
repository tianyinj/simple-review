<template name="text-block0">
    <view class="meta">
      <text class="content">{{item.text}}</text>
    </view>
</template>

<template name="text-block1">
    <view class="meta">
      <text class="content">{{item.nodes[0].text}}</text>
    </view>
</template>

<template name="review-block">
  <view class="list" wx:if="{{renderType == 1}}">
    <template is="text-block1" data="{{item}}" wx:for="{{article}}" wx:key="id"/>
  </view>
  <view class="list" wx:if="{{renderType == 0}}">
    <template is="text-block0" data="{{item}}" wx:for="{{article}}" wx:key="id"/>
  </view>
</template>


<template is="review-block" data="{{article:article, renderType:renderType}}"/>